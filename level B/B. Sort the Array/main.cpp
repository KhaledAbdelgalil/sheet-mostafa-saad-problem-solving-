#include <bits/stdc++.h>

using namespace std;
stack<int>s;
int main()
{
    int n;
    cin>>n;
    int arr[n+1];
    for(int i=1;i<=n;i++)
    {
        cin>>arr[i];
        if(arr[i]!=i)s.push_back()
    }

    return 0;
}
